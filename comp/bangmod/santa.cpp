#include <iostream>
#include <vector>

int main() {
	int n;std::cin >> n;
	std::vector<int> bingo(9);
	std::vector<int> sant;
	std::vector<int> deer;
	for(size_t i=0;i<bingo.size();i++) {
		bingo[i] = i;
	}
	for(size_t i=0;i<bingo.size();i++) {
		if(i%2==0)
			sant[i] = 
	}
	
}