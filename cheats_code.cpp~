#include <string>
#include <iostream>
int main() {
	int line;
	std::cin >> line;
	line++;
	std::string output = "";
	while(line--) {
		std::string code;""
		std::cin >> code;
		int count=0;
		for(int i=1;i<=code.size();i++) {
			if(i == 17 || i%17 == 0) {
				output+='+';	
			}
			output+=code[i-1];
		}
	}
	std::cout << "output " << output << std::endl;
}
