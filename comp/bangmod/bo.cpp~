#include <vector>
#include <iostream>

std::vector<int> fibonacci_member(int n){ // output member of fibonacci
	std::vector<int> ans;
    int init = 0, sec = 1, next = 0;
    next = init + sec;
    //cout << init << " " << sec << " ";
    while(next<=n){
        //cout << next << " ";
        init=sec;
        sec=next;
		ans.push_back(next);
        next=init+sec;
    }
	ans.push_back(next);
    return ans;
}
int main() {
	std::vector<int> vec (4,0);
	//int n; std::cin >> n;
	std::vector<int> res = fibonacci_member(6);
	for(int i:res)
		std::cout << i <<std::endl;
}
